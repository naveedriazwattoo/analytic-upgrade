const routes = Object.freeze({
    ROUTE_LOGIN: "/" as const,
    ROUTE_DASHBOARD: '/dashboard' as const,
    ROUTE_TOKENS: '/tokens' as const,
    ROUTE_PAGE_NOTE_FOUND: "*" as const,
});
export { routes };
